@model List<ImageViewModel>

@{
    Layout = "/Views/Shared/_LayoutGallery.cshtml";
}

<div>
    Смотри какие есть кратинки
</div>
<div>
    @foreach (var image in Model)
    {
        <a href="/Gallery/AddImage?id=@image.Id">@image.Name</a>
    }
</div>

